This is a sample code for the Seleniumistas meetup on July 10th. 

# How to Run from command line using mvn
* `cd <to directory where pom.xml is saved>`
* `mvn clean test -Dsurefire.XmlSuiteFiles=testng.xml`


